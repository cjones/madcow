# optional extra tools
    email_gateway.py    - takes mail from a pipe and feeds to bot gateway
    madcow.cron         - cronjob script to check madcow health
    mkbin.py            - generates a single compiled madcow binary
    rc.madcow           - init script to stop/start/restart madcow
    release.sh          - branch and release (devel only)
    tagstruct.py        - analyzes HTML and reports tag structure
    test.py             - unit test suite (copy to modules/)

# module dependencies
    utils.py            - core library objects for modules
    wiki.py             - parse MediaWiki pages (wikipedia, etc.)
    twitter.py          - google's twitter API
    useragent.py        - mimic a browser (user agent, cookies, etc)
    throttle.py         - throttling library to prevent abuse
    irclib.py           - implements the IRC protocol
    rssparser.py        - mark pilgrim's liberal RSS parsing library
    shell.py            - simple shell with history for cly protocol
    BeautifulSoup.py    - robust HTML parser/scraper library
    authlib.py          - implements a passwd DB
    colorlib.py         - implements ANSI and mIRC colors
    google.py           - calculator, i'm feeling lucky, spellcheck
    pyfiglet.py         - figlet rendering library
    fonts.zip           - collection of figlet fonts
    grufti-responses.txt-insub  - #insub's obnoxious grufti responses
